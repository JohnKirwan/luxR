% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_lux_spectrum.R
\name{as_lux_spectrum}
\alias{as_lux_spectrum}
\alias{as_lux_spectrum.numeric}
\alias{as_lux_spectrum.data.frame}
\alias{as_lux_spectrum.lux_spectrum}
\alias{as_lux_spectrum.lux_instrument_spectrum}
\alias{as_lux_spectrum.rspec}
\alias{as_lux_spectrum.source_spct}
\alias{as_lux_spectrum.reflector_spct}
\title{Coerce an object to lux_spectrum}
\usage{
as_lux_spectrum(x, ...)

\method{as_lux_spectrum}{numeric}(
  x,
  lambda,
  quantity = "irradiance",
  unit = "W/m2/nm",
  binwidth = NULL,
  meta = list(),
  ...
)

\method{as_lux_spectrum}{data.frame}(
  x,
  lambda_col = NULL,
  E_col = NULL,
  quantity = "irradiance",
  unit = "W/m2/nm",
  binwidth = NULL,
  meta = list(),
  ...
)

\method{as_lux_spectrum}{lux_spectrum}(x, ...)

\method{as_lux_spectrum}{lux_instrument_spectrum}(
  x,
  quantity = NULL,
  unit = NULL,
  calibration = NULL,
  negative_policy = "error",
  ...
)

\method{as_lux_spectrum}{rspec}(
  x,
  column = 2,
  quantity = "irradiance",
  unit = "W/m2/nm",
  meta = list(),
  ...
)

\method{as_lux_spectrum}{source_spct}(
  x,
  measurement = NULL,
  negative_policy = "error",
  interpolate = FALSE,
  ...
)

\method{as_lux_spectrum}{reflector_spct}(x, negative_policy = "error", interpolate = FALSE, ...)
}
\arguments{
\item{x}{Object to coerce.}

\item{...}{Additional arguments passed to methods.}

\item{lambda}{Wavelength vector in nm (required for numeric method).}

\item{quantity}{Physical quantity string. Default \code{"irradiance"}.}

\item{unit}{Unit string from the controlled vocabulary. Default \code{"W/m2/nm"}.}

\item{binwidth}{Bin width in nm; inferred if \code{NULL}.}

\item{meta}{Named list of metadata.}

\item{lambda_col}{Column name for wavelengths. Auto-detected from
\code{lambda} > \code{wavelength} > \code{wv} if \code{NULL}.}

\item{E_col}{Column name for spectral values. Auto-detected from
\code{E} > \code{irradiance} > \code{radiance} > \code{value} if \code{NULL}.}

\item{calibration}{For an instrument spectrum, a non-empty calibration
identifier or description.}

\item{negative_policy}{For an instrument spectrum, either error on negative
processed values or explicitly replace them with zero and record the
affected indices.}

\item{column}{For the \code{rspec} method (\pkg{pavo}), which spectrum column
to convert: a column name or index. Default the first spectrum (column 2,
after \code{wl}).}

\item{measurement}{For a photobiology \code{source_spct} carrying both an
energy and a photon column, which to import: \code{"energy"} or
\code{"photon"}. Required only when the choice is ambiguous.}

\item{interpolate}{For a photobiology spectrum, whether an irregular
wavelength grid may be resampled onto integer nanometres. Defaults to
\code{FALSE}, so irregular grids fail with guidance to opt in.}
}
\value{
A \code{lux_spectrum} object.
}
\description{
Converts a numeric vector or data frame to a \code{\link{lux_spectrum}}
object. The data-frame method auto-detects the wavelength column
(\code{lambda} > \code{wavelength} > \code{wv}) and the spectral-value
column (\code{E} > \code{irradiance} > \code{radiance} > \code{value}).
Passing an existing \code{lux_spectrum} returns it unchanged.
}
\examples{
# from a numeric vector
lam <- seq(400, 700, by = 10)
as_lux_spectrum(rep(1, length(lam)), lambda = lam)

# from a data frame
df <- data.frame(lambda = lam, E = rep(1, length(lam)))
as_lux_spectrum(df)
}
\seealso{
\code{\link{lux_spectrum}}, \code{\link{from_naples}},
  \code{\link{from_solar}}
}
