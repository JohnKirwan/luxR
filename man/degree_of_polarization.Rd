% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polarization.R
\name{degree_of_polarization}
\alias{degree_of_polarization}
\title{Degree of polarization from Stokes parameters}
\usage{
degree_of_polarization(I, Q, U, V = 0)
}
\arguments{
\item{I}{Total radiance (Stokes I). Must be > 0.}

\item{Q, U}{Linear polarization Stokes parameters.}

\item{V}{Circular polarization Stokes parameter. Default 0.}
}
\value{
Degree of polarization in \code{[0, 1]}. Vectorised. Scalar inputs
  are broadcast; incompatible non-scalar lengths are rejected.
}
\description{
\deqn{DoP = \sqrt{Q^2 + U^2 + V^2} / I}. With \code{V = 0} (the default, and
the usual case for the underwater light field, which is essentially linearly
polarized) this is the degree of \emph{linear} polarization.
}
\details{
All inputs must be finite numeric vectors. A Stokes vector is
  physical only when \eqn{I > 0} and
  \eqn{\sqrt{Q^2 + U^2 + V^2} \le I}. Values exceeding this bound by more
  than 64 machine epsilons are rejected with a
  \code{luxR_polarization_physicality_error}. Values within that round-off
  tolerance are returned as exactly one.
}
\examples{
degree_of_polarization(I = 1, Q = 0.4, U = 0)   # 0.4
}
\seealso{
\code{\link{angle_of_polarization}}, \code{\link{underwater_polarization}}
}
