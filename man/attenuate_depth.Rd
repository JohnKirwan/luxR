% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth_propagation.R
\name{attenuate_depth}
\alias{attenuate_depth}
\title{Beer-Lambert attenuation at a single wavelength.}
\usage{
attenuate_depth(E0, Kd, depth)
}
\arguments{
\item{E0}{Surface irradiance. Any unit; result is in the same unit.}

\item{Kd}{Diffuse attenuation coefficient in 1/m. Must be >= 0.}

\item{depth}{Depth(s) in metres. Must be >= 0. Scalar or vector.}
}
\value{
Numeric vector the same length as \code{depth}.
}
\description{
Computes E(z) = E0 * exp(-Kd * z). Vectorised over \code{depth}.
}
\details{
This is the package's principal lightweight water-column model. It
  assumes that \eqn{K_d} is constant over the modeled path and propagates
  each wavelength independently. It does not solve the angular radiance
  field, multiple scattering, or wavelength redistribution. The scalar API
  has no inherent wavelength or depth range; the caller is responsible for
  supplying a \eqn{K_d} that is valid for the medium and path.
}
\examples{
attenuate_depth(100, Kd = 0.06, depth = c(0, 10, 25, 50))
}
