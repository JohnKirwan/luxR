% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth_propagation.R
\name{propagate_spectrum}
\alias{propagate_spectrum}
\alias{propagate_spectrum.numeric}
\alias{propagate_spectrum.lux_spectrum}
\title{Propagate a known spectrum to any other depth(s) (bidirectional).}
\usage{
propagate_spectrum(E_lambda, ...)

\method{propagate_spectrum}{numeric}(
  E_lambda,
  Kd_lambda,
  from = 0,
  to,
  format = c("long", "matrix"),
  lambda = NULL,
  allow_above_surface = FALSE,
  ...
)

\method{propagate_spectrum}{lux_spectrum}(
  E_lambda,
  Kd_lambda,
  from = 0,
  to,
  allow_above_surface = FALSE,
  ...
)
}
\arguments{
\item{E_lambda}{Spectral irradiance at depth \code{from}. Length-N vector
or a \code{lux_spectrum} object.}

\item{...}{Ignored.}

\item{Kd_lambda}{Finite, non-negative diffuse attenuation coefficients
(1/m). Length-N vector.}

\item{from}{Finite scalar depth of the known spectrum in metres. Default
0.}

\item{to}{Finite target depth(s) in metres. Scalar or vector.}

\item{format}{\code{"long"} (default) or \code{"matrix"}. Ignored for
\code{lux_spectrum} input.}

\item{lambda}{Optional wavelength values (nm) for long-format output.}

\item{allow_above_surface}{Logical; permits negative absolute depths.
Default FALSE.}
}
\value{
A data frame, matrix, or named list of \code{lux_spectrum}.
}
\description{
Applies Beer-Lambert attenuation at each wavelength independently,
propagating a known spectral irradiance from depth \code{from} to one or
more target depths \code{to}. Works in both directions: set \code{to <
from} to recover a shallower (or surface) spectrum from a deeper
measurement, assuming a homogeneous water column.
}
\details{
This is the package's principal lightweight propagation tier. It
  applies \eqn{E(z,\lambda) = E(z_0,\lambda)
  \exp[-K_d(\lambda)(z-z_0)]}, assuming \eqn{K_d} is constant along the
  path and wavelengths propagate independently. It does not model an
  angular radiance field, multiple scattering, or spectral redistribution.
  There is no universal supported depth range: validity depends on whether
  the supplied \eqn{K_d} represents the entire path. Bundled Jerlov data are
  limited to 350--700 nm. Inverse propagation is an assumption-heavy
  reconstruction that amplifies noise.
}
\examples{
E_10m <- c(8, 15, 20)
Kd    <- c(0.02, 0.06, 0.25)
propagate_spectrum(E_10m, Kd, from = 10, to = c(0, 25, 50))
}
\references{
Kirk JTO (1994) Light and Photosynthesis in Aquatic Ecosystems,
  2nd edn. Cambridge University Press.
}
