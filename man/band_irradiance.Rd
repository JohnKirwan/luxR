% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/band_irradiance.R
\name{band_irradiance}
\alias{band_irradiance}
\alias{band_irradiance.numeric}
\alias{band_irradiance.lux_spectrum}
\title{Band-integrated irradiance at one or more depths}
\usage{
band_irradiance(E_lambda, ...)

\method{band_irradiance}{numeric}(
  E_lambda,
  Kd_lambda,
  lambda,
  depths,
  from = 0,
  lambda_min = NULL,
  lambda_max = NULL,
  photonic = FALSE,
  molar_unit = "umol",
  binwidth = NULL,
  allow_above_surface = FALSE,
  ...
)

\method{band_irradiance}{lux_spectrum}(E_lambda, Kd_lambda, depths, lambda = NULL, ...)
}
\arguments{
\item{E_lambda}{Spectral irradiance (\eqn{W\,m^{-2}\,nm^{-1}}) at depth \code{from}.
Length-N vector, or a \code{lux_spectrum} object.}

\item{...}{Ignored.}

\item{Kd_lambda}{Diffuse attenuation coefficients (1/m) at the same
wavelengths. Length-N vector.}

\item{lambda}{Wavelengths in nm. Length-N vector (not needed when
\code{E_lambda} is a \code{lux_spectrum}).}

\item{depths}{Target depth(s) in metres. Scalar or vector.}

\item{from}{Depth of the known spectrum in metres. Default \code{0}
(surface). Set to a positive value to propagate from a subsurface
measurement.}

\item{lambda_min}{Lower wavelength bound in nm (inclusive). Defaults to
\code{min(lambda)}.}

\item{lambda_max}{Upper wavelength bound in nm (inclusive). Defaults to
\code{max(lambda)}.}

\item{photonic}{Logical; if \code{TRUE} the result is returned in
\eqn{\mu}mol photons m\eqn{^{-2}} s\eqn{^{-1}} rather than \eqn{W\,m^{-2}}.
Default \code{FALSE}.}

\item{molar_unit}{Molar unit for photonic output. Default \code{"umol"}.}

\item{binwidth}{Wavelength bin width in nm. Inferred from \code{lambda}
if not supplied.}

\item{allow_above_surface}{Logical; permits target depths above the surface
(negative values). Default \code{FALSE}.}
}
\value{
A \code{data.frame} with columns:
  \describe{
    \item{depth}{Target depth in metres.}
    \item{E}{Band-integrated irradiance in \eqn{W\,m^{-2}} (or \eqn{\mu}mol m\eqn{^{-2}} s\eqn{^{-1}}
      when \code{photonic = TRUE}).}
  }
}
\description{
Propagates a spectrum from a known depth to one or more target depths via
Beer-Lambert attenuation and integrates the result over a wavelength band.
Works in both directions: set \code{from} > \code{depths} to recover
shallower values (including the surface) from a deeper measurement.
}
\details{
Two operations are applied in sequence. First, Beer-Lambert propagation at
each wavelength:
\deqn{E(z,\,\lambda) = E_0(\lambda)\,
      \exp\!\bigl[-K_d(\lambda)\,(z - z_0)\bigr]}
where \eqn{z_0} is the depth of the known spectrum (\code{from}) and
\eqn{K_d(\lambda)} is the diffuse attenuation coefficient. Second, the
propagated spectrum is integrated over the specified wavelength window
[\eqn{\lambda_{\min}}, \eqn{\lambda_{\max}}]:
\deqn{E_{\mathrm{band}}(z) =
      \int_{\lambda_{\min}}^{\lambda_{\max}} E(z,\,\lambda)\,d\lambda}
approximated as \eqn{\sum_i E(z,\lambda_i)\,\Delta\lambda} over bins within
the window. See \code{\link{propagate_spectrum}} for the full Beer-Lambert
documentation.
}
\examples{
sp  <- solar_irradiance("clear_noon")
keep <- sp$wavelength >= 350 & sp$wavelength <= 700
lam <- sp$wavelength[keep]
E0  <- sp$irradiance[keep]
Kd  <- jerlov_Kd("II", lam)

# surface to depth profile, 500-600 nm band
band_irradiance(E0, Kd, lam, depths = c(0, 5, 10, 20, 50),
                lambda_min = 500, lambda_max = 600)

# recover surface from a 10 m measurement
E_10m <- E0 * exp(-Kd * 10)
band_irradiance(E_10m, Kd, lam, depths = 0, from = 10)
}
\seealso{
\code{\link{propagate_spectrum}}, \code{\link{par_irradiance}}
}
