% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/species_perception.R
\name{species_LEF}
\alias{species_LEF}
\title{Spectral sensitivity for a named species and photoreceptor class.}
\usage{
species_LEF(species, receptor = NULL, lambda = seq(300, 700, by = 1))
}
\arguments{
\item{species}{Species name (character). Must match a row in
\code{species_sensitivities$species}. Case-sensitive.}

\item{receptor}{Photoreceptor class (e.g. \code{"L-cone"}, \code{"rod"}).
If \code{NULL} (default), returns all receptor classes as a named list.}

\item{lambda}{Wavelength grid in nm. Default \code{seq(300, 700, by = 1)}.}
}
\value{
A data frame (single receptor) or named list of data frames (all
  receptors). Each data frame has columns \code{lambda} and \code{S}.
}
\description{
Looks up peak wavelength and chromophore from the bundled
\code{species_sensitivities} table, then applies the Govardovskii (2000)
template to return a normalised sensitivity curve.
}
\examples{
species_LEF("Homo sapiens", receptor = "L-cone")
species_LEF("Danio rerio")
}
