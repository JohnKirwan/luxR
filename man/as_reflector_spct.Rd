% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/photobiology_interop.R
\name{as_reflector_spct}
\alias{as_reflector_spct}
\alias{as_reflector_spct.lux_spectrum}
\alias{as_reflector_spct.list}
\title{Convert luxR reflectance to a photobiology reflector_spct}
\usage{
as_reflector_spct(x, ...)

\method{as_reflector_spct}{lux_spectrum}(x, Rfr.type = NULL, strict.range = TRUE, ...)

\method{as_reflector_spct}{list}(x, ...)
}
\arguments{
\item{x}{A reflectance \code{\link{lux_spectrum}}, or a (named) list of them.}

\item{...}{Passed to \code{photobiology::reflector_spct()}.}

\item{Rfr.type}{Either \code{"total"} or \code{"specular"}. Defaults to
\code{x$meta$Rfr_type}; an error is raised if neither is supplied.}

\item{strict.range}{Passed to \code{photobiology::reflector_spct()}.
Defaults to \code{TRUE}.}
}
\value{
A \pkg{photobiology} \code{reflector_spct}.
}
\description{
Bridges a reflectance \code{\link{lux_spectrum}} into \pkg{photobiology} as a
\code{reflector_spct}. luxR already constrains reflectance to \code{[0, 1]},
so values map onto \code{Rfr} directly.
}
\details{
\code{Rfr.type} is required. Whether a reflectance is total or specular is a
fact about how it was measured, which luxR does not otherwise carry;
defaulting it would silently mislabel the measurement inside another
package's ecosystem. Record it once in the spectrum's
\code{meta$Rfr_type} to have it picked up automatically.
}
\examples{
\dontrun{
  r <- as_reflector_spct(my_reflectance, Rfr.type = "total")
}
}
\seealso{
\code{\link{as_source_spct}}, \code{\link{as_lux_spectrum}}
}
