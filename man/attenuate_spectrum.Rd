% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth_propagation.R
\name{attenuate_spectrum}
\alias{attenuate_spectrum}
\alias{attenuate_spectrum.numeric}
\alias{attenuate_spectrum.lux_spectrum}
\title{Propagate a surface spectrum through the water column.}
\usage{
attenuate_spectrum(E0_lambda, ...)

\method{attenuate_spectrum}{numeric}(
  E0_lambda,
  Kd_lambda,
  depths,
  format = c("long", "matrix"),
  lambda = NULL,
  ...
)

\method{attenuate_spectrum}{lux_spectrum}(E0_lambda, Kd_lambda, depths, ...)
}
\arguments{
\item{E0_lambda}{Surface spectral irradiance. Length-N numeric vector or
a \code{lux_spectrum} object.}

\item{...}{Ignored.}

\item{Kd_lambda}{Finite, non-negative diffuse attenuation coefficients
(1/m) at the same wavelength bins as \code{E0_lambda}. Length-N numeric
vector.}

\item{depths}{Finite, non-negative depths to evaluate in metres. Length-M
numeric vector.}

\item{format}{\code{"long"} (default): tidy data frame with columns
\code{depth}, \code{lambda}, \code{E}. \code{"matrix"}: N x M numeric
matrix. Ignored for \code{lux_spectrum} input.}

\item{lambda}{Optional wavelength values (nm) for the \code{lambda}
column of the long-format output. Defaults to bin indices 1:N.}
}
\value{
A data frame, matrix, or named list of \code{lux_spectrum}.
}
\description{
Applies per-wavelength Kd across a depth grid using the Beer-Lambert law.
Returns a tidy data frame, a numeric matrix, or (for lux_spectrum input)
a named list of lux_spectrum objects.
}
\details{
This is a wavelength-resolved Beer-Lambert calculation, not a
  radiative-transfer solution. It assumes a homogeneous \eqn{K_d(\lambda)}
  over the modeled path, treats wavelengths independently, and accepts only
  irradiance when a \code{lux_spectrum} is supplied. The generic numeric API
  cannot verify physical quantity or wavelength alignment; callers must
  supply aligned irradiance and \eqn{K_d} vectors. Bundled Jerlov
  coefficients are supported only from 350--700 nm; see
  \code{\link{jerlov_Kd}}.
}
\examples{
E0  <- c(10, 20, 30)
Kd  <- c(0.01, 0.05, 0.20)
lam <- c(450, 550, 650)
attenuate_spectrum(E0, Kd, depths = c(0, 10, 25), lambda = lam)
}
\references{
Kirk JTO (1994) Light and Photosynthesis in Aquatic Ecosystems,
  2nd edn. Cambridge University Press.
}
\seealso{
\code{\link{propagate_spectrum}}, \code{\link{band_irradiance}},
  \code{\link{jerlov_Kd}}
}
